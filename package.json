{
  "name": "procurement-saas",
  "version": "1.0.0",
  "private": true,
  "description": "",
  "main": "index.js",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "test": "npm run test --workspaces --if-present",
    "dev": "echo 🚀 FRONTEND IS RUNNING ON: http://localhost:3000 && concurrently \"npm run dev -w api\" \"npm run dev -w web\"",
    "build": "npm run build --workspaces --if-present"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@prisma/client": "^6.19.2",
    "concurrently": "^9.2.1",
    "dotenv": "^17.3.1",
    "prisma": "^6.19.2"
  }
}